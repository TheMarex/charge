#ifndef CHARGE_EXPERIMENTS_HPP
#define CHARGE_EXPERIMENTS_HPP

namespace charge::experiments {

inline struct sequential_t final { } sequential;
inline struct parallel_t final { } parallel;

}

#endif
